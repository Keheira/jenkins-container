pipeline {
  agent any
  stages {
    stage('test') {
      steps {
        echo 'No test but this would be where it happens'
      }
    }

    stage('deploy') {
      steps {
        echo 'Done'
      }
    }

  }
}